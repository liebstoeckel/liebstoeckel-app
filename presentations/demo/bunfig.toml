# Plugins for the HMR dev server (Bun.serve HTML routes). The build path uses
# Bun.build()'s plugins array in build.ts instead (CLI/serve configs differ).
[serve.static]
plugins = ["bun-plugin-tailwind", "../../packages/engine/src/build/mdx-plugin.ts"]
