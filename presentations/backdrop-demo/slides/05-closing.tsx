import { ClosingSlide } from "@liebstoeckel/components";

export const notes = "Press O for the overview: every thumbnail shows the same sky, frozen. That is the still contract doing its job.";

export default function Closing() {
  return (
    <ClosingSlide
      title="Your deck, your weather"
      subtitle="Press O: the overview freezes this sky into every thumbnail."
      url="liebstoeckel.app"
    />
  );
}
