// Subpath entry for the in-frame bridge (`@liebstoeckel/dev-server/bridge`).
// Lives beside the other drawer sources; this file only re-exports so the
// package export map and the repo's path aliases both point at `src/`.
export * from "../drawer/bridge";
